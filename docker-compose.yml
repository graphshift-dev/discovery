version: '3.8'

services:
  graphshift:
    build: .
    container_name: graphshift-discovery
    volumes:
      - ./outputs:/app/outputs
      - ./config:/app/config
    environment:
      - PYTHONPATH=/app
    # Example usage - uncomment and modify as needed
    # command: ["analyze", "--repo", "https://github.com/spring-projects/spring-petclinic", "--to-version", "17"]